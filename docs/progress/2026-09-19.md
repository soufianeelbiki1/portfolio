# Progress log — 2026-09-19

Budget remained zero. AtlasPay main received three workflow-only dependency
updates. No deployment, running container, credential, billing setting, database
or paid resource changed. The two AtlasPay services' Railway build-trigger
rules correctly skipped all three commits.

## Reviewed before changes

- Rechecked all 16 scoped open pull requests, their latest diffs, checks,
  comments and review threads. Portfolio #11, RetailIntel #6, AtlasPay #38 and
  Nexus #26 remain conflict-free with successful exact-head CI and no unresolved
  human review feedback.
- Nexus dependency PR #29 is green. #27 and #28 are red because Dependabot split
  peer-dependent React type/runtime upgrades across separate branches. They were
  not merged, rerun or represented as release-ready. The Node 26 container
  suggestion remains outside the reviewed Node 24 runtime alignment.

## Nexus public-preview verification

- Exact review head: `372a9a41607cd07c59e4cfefbecc7c046383a313`.
- [Preview](https://nexus-mchodvzdz-soufiane15.vercel.app/) state: READY. Vercel
  production remains on the older main revision.
- A real 1363×936 browser rendered the authenticated `atlaspay-api` snapshot,
  eight synthetic network observations, semantic headings/regions and no
  horizontal overflow.
- Reloading advanced the producer timestamp from 09:08:29 to 09:09:33. This
  verifies runtime refresh behavior, not real payment traffic or adoption.
- Vercel returned no runtime error cluster and no warning/error/fatal log for
  the exact preview in the preceding 24 hours. The only browser-console errors
  came from the browser's own extension, not the application origin.
- The authenticated live dashboard intentionally omits identifier-bearing
  transaction drill-down. Search/outcome filters belong to fixture-development
  mode and their predicates pass CI; they are not claimed as live interactions.

## AtlasPay deployment-budget boundary

- [AtlasPay #38](https://github.com/soufianeelbiki1/AtlasPay/pull/38) now records
  the live monorepo release boundary at
  `968bd5703360f346438ed6c7b2afb1c24d197a7d`.
- The Java service watches only its Dockerfile, Maven descriptor, Railway
  config and `src/main/**`. The Python API watches its Docker/build config,
  `app/**`, `migrations/**` and `pyproject.toml`. These repository-root patterns
  follow Railway's documented
  [monorepo watch-path behavior](https://docs.railway.com/deployments/monorepo#watch-paths).
- Re-reading both service configs confirmed the saved allowlists. Re-reading
  deployment history confirmed the update created no build or restart: the
  latest Java deployment remains `2b26acba-52b1-4f15-95f0-a25cc1afc017` and
  the latest Python deployment remains
  `5bc62e6b-80c2-4b78-8dee-bd0fbb45bdb2`, both successful on the unchanged
  September 8 main revision.
- Seven-day Railway metrics show both services are active, averaging about
  0.22 GB and 0.055 GB of memory respectively. Current remaining trial credit
  is not exposed by the connected interface. Both services still use
  `checkSuites=false`; watch paths reduce duplicate builds but do not create a
  CI release gate.
- [Java CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35436564888)
  passes all 36 tests with zero skips. [Root CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35436564884)
  passes lint, migrations, all 105 PostgreSQL tests and container checks on the
  exact documented head.

## CI action upgrades and live watch-path proof

- Rechecked all 14 scoped open pull requests at their latest heads, including
  diffs, checks, comments, reviews and unresolved review threads. The
  substantive portfolio, AtlasPay, Nexus and RetailIntel branches remain
  conflict-free with successful exact-head CI and no pending human feedback.
- Merged [AtlasPay #30](https://github.com/soufianeelbiki1/AtlasPay/pull/30) at
  `cfc0f6058d2c416187c87e4965d8ba971f3d1913`. This changes only the Java CI
  workflow from `actions/setup-java@v4` to `@v6`; Java 21, Temurin and Maven
  caching remain unchanged. The upstream
  [v6.0.0 release](https://github.com/actions/setup-java/releases/tag/v6.0.0)
  documents its Node 24 action-runtime migration and no user-facing breaking
  change.
- [Post-merge Java CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35450638739)
  ran `actions/setup-java@v6` and passed the main-branch Java suite with zero
  failures or skips. [Post-merge root CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35450638713)
  passed lint, migrations, all 104 main-branch PostgreSQL tests and container
  checks.
- Railway recorded the same commit as `SKIPPED` for Java
  (`a7a4cd49-43b2-4db1-b0ec-be33d5a1d5cd`) and Python
  (`17095157-ead9-4001-8280-0fc21d513a5c`). The active deployments remain the
  successful September 8 revision `7990d04f2485b9cf46ab5c540b0418a128b48a7b`.
  This proves the saved watch paths prevent a workflow-only merge from building
  or restarting either service; it does not turn `checkSuites=false` into a CI
  release gate.
- Merged [AtlasPay #24](https://github.com/soufianeelbiki1/AtlasPay/pull/24)
  at `fa662a25fa429a8129a4497a242a856039bee61d`. Its two one-line changes move
  root CI and security checks from `actions/setup-python@v5` to `@v7` while
  preserving Python 3.11 and pip caching. The official
  [v7.0.0 release](https://github.com/actions/setup-python/releases/tag/v7.0.0)
  records its ESM/dependency refresh and manifest-fetch validation/retry fix;
  the removed `pip-install` input was not used by AtlasPay.
- [Post-merge root CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35454293378)
  downloaded `setup-python@v7`, passed lint, formatting, migrations, all 104
  main-branch PostgreSQL tests and the non-root container check.
  [Post-merge security CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35454293388)
  passed dependency consistency/audit with no known vulnerabilities and passed
  its runtime-image health smoke.
- Railway recorded #24 as `SKIPPED` for Java
  (`caf824ce-43be-45aa-8e66-f24401f7dec5`) and Python
  (`b6b18ff8-53cf-4221-a68b-bc71c2037d89`). Their active deployments still
  point to the September 8 runtime revision.
- Merged [AtlasPay #23](https://github.com/soufianeelbiki1/AtlasPay/pull/23)
  at `e9e46c022e756c0bc60dca2a3229aacc2e79525b`. Five existing checkout steps
  across root, Java and security workflows now use `actions/checkout@v7`. The
  official [v7.0.0 release](https://github.com/actions/checkout/releases/tag/v7.0.0)
  uses the Node 24 action runtime and adds a guard against checking out fork
  pull requests from privileged `pull_request_target` or `workflow_run`
  contexts.
- [Post-merge root CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35456732363)
  downloaded `checkout@v7`, passed lint, formatting, migrations, all 104
  main-branch PostgreSQL tests and the non-root image check.
  [Java CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35456732405)
  passed the five main-branch Java tests with zero skips, and
  [security CI](https://github.com/soufianeelbiki1/AtlasPay/actions/runs/35456732372)
  passed dependency audit and runtime health smoke.
- Railway recorded #23 as `SKIPPED` for Java
  (`93e93f8a-c31d-4a4c-a5ad-aef12d5abb4b`) and Python
  (`ffae69c8-516b-4d64-a35e-e519b9177c47`). Their active September 8
  deployments again remained unchanged.
- Railway's documented [Wait for CI](https://docs.railway.com/deployments/github-autodeploys#wait-for-ci)
  prerequisites are present: the repository has push-to-main workflows and
  both services are GitHub-linked. The available configuration path staged the
  setting as an environment change that would require accepting a deployment.
  It was not accepted: the staged patch was discarded, both active services
  were re-read unchanged, and `checkSuites=false` remains an explicit blocker.
- After the merge, the public [Nexus preview](https://nexus-mchodvzdz-soufiane15.vercel.app/)
  still rendered authenticated `atlaspay-api` data rather than fixtures, had no
  horizontal overflow at 1363×936 and emitted no application warning or error.
  Its producer timestamp refreshed to 15:05:57 on 2026-09-19. The fixed browser
  viewport still cannot establish mobile behavior.

## Dependency queue curation

- Rechecked all 13 remaining open pull requests at their exact heads, including
  file diffs, workflow runs, conversation comments, submitted reviews and
  unresolved review threads. No human feedback was pending.
- Closed [Nexus #11](https://github.com/soufianeelbiki1/Nexus/pull/11)
  without merging. Its direct Node 22 → 26 container jump conflicts with the
  reviewed Node 24 runtime alignment in #26 and omits that branch's lockfile and
  `npm ci` work. Its old CI result does not establish a coherent Node 26
  migration.
- Closed [Nexus #27](https://github.com/soufianeelbiki1/Nexus/pull/27)
  without merging. It moves only `@types/node` to 26 while the reviewed runtime
  remains Node 24, and CI, security and integrated-demo checks all fail.
- Closed [Nexus #28](https://github.com/soufianeelbiki1/Nexus/pull/28)
  without merging. The isolated `react-dom@19.3.0` bump leaves React at 19.2.8,
  so all three check groups fail dependency resolution. A future React 19.3
  change must move the compatible runtime/type set together on the reproducible
  lockfile branch.
- Ten pull requests remained open after the Nexus cleanup. A fresh recheck
  reported all ten mergeable; the earlier #22/#25 conflict state was transient
  and is corrected below. Their exact-head workflows remained green.

## AtlasPay dependency release-boundary cleanup

- Rechecked all ten remaining pull requests, latest diffs, checks, comments,
  reviews and review threads. All were mergeable with green exact-head
  workflows and no unresolved human feedback.
- Closed [AtlasPay #22](https://github.com/soufianeelbiki1/AtlasPay/pull/22)
  without merging. The direct Python 3.11 → 3.14 runtime jump had only an
  August 31 branch result, while current main and the running service retain the
  reviewed Python 3.11 baseline. Because `Dockerfile` is watched and Railway
  still uses `checkSuites=false`, merging would automatically deploy against
  the finite trial allowance.
- Closed [AtlasPay #25](https://github.com/soufianeelbiki1/AtlasPay/pull/25)
  without merging. It changes only the development pytest range, but
  `pyproject.toml` is watched by the Python service, so the merge would spend a
  deployment on a change with no runtime feature.
- Eight pull requests remain open; all are mergeable with green exact-head
  workflows. No default branch, Railway deployment or paid resource changed.
- Reverified the [Nexus #26 preview](https://nexus-mchodvzdz-soufiane15.vercel.app/)
  in a real 1363×936 browser. It rendered authenticated `atlaspay-api` data,
  semantic regions and a 1,348 px document inside the 1,363 px viewport, with
  no application-origin warning/error. The browser still cannot create a
  phone-sized viewport, so this is desktop evidence only.

## RetailIntel artifact provenance

- Rechecked all eight open pull requests at their exact heads, including diffs,
  workflow runs, comments, submitted reviews and review threads. Every head was
  mergeable with green checks and no unresolved human feedback. AtlasPay #37
  and Nexus #25 remain merged into their review branches rather than main.
- Extended existing [RetailIntel #6](https://github.com/soufianeelbiki1/RetailIntel/pull/6)
  at `d02ec9a2f540bc438f87b4a32b414057429640d8`. The evaluator and dashboard now
  share central synthetic seed/order defaults and accept the same CLI options.
  The dashboard visibly records those inputs, demand-history range, inventory
  snapshot and evaluation cutoff, so a reviewer can detect mismatched evidence.
- A caller-provided DuckDB connection cannot be mislabeled with generator
  inputs. Invalid order counts fail before replacing an existing dashboard.
  A seed-99, 30-order local run produced matching JSON source metadata and HTML
  provenance without a hosted database, model or API.
- Local Ruff lint/format, packaging/installed-wheel execution and all 35 tests
  pass. [Exact-head CI](https://github.com/soufianeelbiki1/RetailIntel/actions/runs/35475376274)
  passes the same suite on Python 3.11 and 3.12.
- The PR remains draft: the supported browser still cannot render the local
  static file at desktop/mobile viewports, so visual and keyboard behavior are
  not claimed. RetailIntel has no connected Vercel project; no deployment or
  paid resource changed.

## Remaining gate

The supported browser exposes a fixed desktop viewport. CSS contains explicit
820 px and 520 px layouts, but that code inspection is not mobile verification.
Nexus #26 therefore remains ready for review but unmerged. Portfolio #11 remains
draft for the same real-browser desktop/mobile, keyboard, reduced-motion and
contact-path gates.
