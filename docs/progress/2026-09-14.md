# Progress — September 14, 2026

## AtlasRAG decision diagnostics

- Rechecked every scoped open PR head, changed-file list, CI result and review
  thread before starting. No head moved and no review feedback was pending.
- Audited AtlasRAG main after #11. Duplicate relevant citations could count as
  repeated hits and inflate recall above 100%; one aggregate evidence-decision
  score also hid whether failures were false abstentions or unsafe evidence.
- Added bounded citation scoring, answerable-grounding and safe-abstention rates,
  explicit failure counts and matching case-level report outcomes in
  [AtlasRAG #12](https://github.com/soufianeelbiki1/AtlasRAG/pull/12).
- Local Python 3.12 checks passed 41 tests; three PostgreSQL tests were skipped
  because this workspace has no database. Dependency, lint, formatting, compile
  and deterministic report-generation checks passed.
- Exact-head and
  [post-merge CI](https://github.com/soufianeelbiki1/AtlasRAG/actions/runs/34795357503)
  each passed all 44 tests on Python 3.11 and 3.12 with PostgreSQL. The reviewed
  five-file change was squash-merged to main at
  `18ac326741cae5db33fabb5f3c6a9b6a1047a025`.
- No model/API call, deployment, hosted database or paid resource was used.

## Browser and release boundaries

- The public Nexus #26 preview still renders authenticated runtime data at the
  supported 1363×936 viewport. The only available browser does not expose a
  mobile viewport, and its URL policy rejected an isolated narrow-width frame.
  Mobile layout and the conditional filter therefore remain unverified.
- Portfolio #11 stays draft. Its source, static AtlasRAG snapshot and case study
  now reference the merged decision diagnostics, but responsive/browser and
  zero-cost commercial-hosting gates remain unchanged.
